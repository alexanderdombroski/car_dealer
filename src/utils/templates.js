/**
 * Returns the navigation menu.
 *
 * @returns {string} The navigation menu.
 */
const getNav = () => {
    const nav = `
        <nav>
            <a href="/account/login">Log In</a>
        </nav>
    `;
    
    return nav;
};

export { getNav };